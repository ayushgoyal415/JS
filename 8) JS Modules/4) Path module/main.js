// Using built-in path module
const path = require("path");

// Displaying what all is provided by path module
console.log(path);

// Using parse function of path module
const A = path.parse(__filename);
console.log(A);
