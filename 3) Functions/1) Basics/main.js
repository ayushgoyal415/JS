// No need to define the return type
// No need to define the parameter types

function sum(a, b) {
	return a + b;
}
console.log(sum(5, 2));

// Declaring a function as a variable
var sum2 = function (a, b) {
	return a + b;
};
console.log(sum2(5, 2));
